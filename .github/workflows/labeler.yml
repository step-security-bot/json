name: "Pull Request Labeler"

on:
  pull_request_target:
    types: [opened, synchronize]

permissions: {}

jobs:
  label:
    permissions:
      contents: read
      pull-requests: write

    runs-on: ubuntu-latest

    steps:
      - uses: srvaroa/labeler@8b4be66d6774f63e9b972c2c831dcfa5ba9c64c2 # master
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
